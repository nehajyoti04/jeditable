jeditable.ajax_save:
  path: '/jeditable/ajax/save'
  defaults:
    _title: 'Save field'
    _controller: '\Drupal\jeditable\Controller\JeditableAjax::jeditable_ajax_save'
  requirements:
    _permission: 'use jeditable'
#jeditable.ajax_load:
#  path: /jeditable/ajax/load
#  defaults:
#    _title: 'Load field'
#    _controller: '\Drupal\jeditable\Controller\DefaultController::_jeditable_ajax_load'
#  requirements:
#    _permission: 'use jeditable'
jeditable.admin_settings:
  path: /admin/config/content/jeditable
  defaults:
    _title: jEditable
    _form: \Drupal\jeditable\Form\JeditableAdminSettings
  requirements:
    _permission: 'administer site configuration'
